{
  "name": "photo-shop",
  "version": "1.0.0",
  "description": "A photo shop, not to be confused with Photoshop ;)",
  "main": "index.js",
  "scripts": {
    "build:frontend": "scripts/build-frontend-site.sh",
    "serve:frontend": "scripts/serve-frontend-site.sh",
    "clean:frontend": "rm -rf _frontend;",
    "build:cloudflare-workers": "scripts/build-all-cloudflare-workers.sh",
    "clean:cloudflare-workers": "rm -rf _cloudflare;",
    "deploy:cloudflare-workers": "scripts/deploy-all-cloudflare-workers.sh",
    "wrangler:list-kvs": "scripts/wrangler-list-kvs.sh"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:MrJman006/gmt-webapps-workshop.git"
  },
  "author": "Joey C. DiGiorgio",
  "devDependencies": {
    "esbuild": "0.17.19",
    "wrangler": "^3.0.0"
  }
}
